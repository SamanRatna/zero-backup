// kanishka dai
    ride_mode = 1
    setInterval(function(){
    	if(ride_mode == 1){
    		ride_mode = 2
    	} else {
    		ride_mode = 1
    	}
    	console.log(ride_mode)
        console.log(pos)

        if (ride_mode == 2) {
            plusSlides(1);
            logstuff()
        }
    }, 5000)
